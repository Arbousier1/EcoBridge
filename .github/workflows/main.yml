name: EcoBridge High-Performance Build (Java 25)

on:
  push:
    branches: [ "main-java25" ]
  pull_request:
    branches: [ "main-java25" ]

env:
  # 1. 全局禁用增量编译，减少 Windows I/O 开销
  CARGO_INCREMENTAL: 0

jobs:
  build-rust:
    name: Native Core (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: libecobridge_rust.so
          - os: windows-latest
            artifact: ecobridge_rust.dll
          - os: macos-latest
            artifact: libecobridge_rust.dylib
    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v4

      # 2. 专项优化：关闭 Windows Defender 实时扫描
      - name: Disable Windows Defender
        if: matrix.os == 'windows-latest'
        run: |
          Set-MpPreference -ExclusionPath ${{ github.workspace }}
        shell: powershell

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.os == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "ecobridge-rust -> target"

      - name: Build Rust
        shell: bash
        # 3. 专项优化：在 Windows 上启用 LLD 链接器
        env:
          RUSTFLAGS: ${{ matrix.os == 'windows-latest' && '-C link-arg=-fuse-ld=lld' || '' }}
        run: |
          cd ecobridge-rust
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            cargo build --release --target x86_64-apple-darwin
            cargo build --release --target aarch64-apple-darwin
            mkdir -p target/release/
            lipo -create \
              target/x86_64-apple-darwin/release/libecobridge_rust.dylib \
              target/aarch64-apple-darwin/release/libecobridge_rust.dylib \
              -output target/release/libecobridge_rust.dylib
          else
            # 使用环境变量中定义的 RUSTFLAGS 进行加速
            cargo build --release
          fi

      - name: Upload Native Artifact
        uses: actions/upload-artifact@v4
        with:
          name: native-${{ matrix.os }}
          path: ecobridge-rust/target/release/${{ matrix.artifact }}

  build-java:
    name: Java 25 Assembly
    needs: build-rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'microsoft'
          cache: 'gradle'

      - name: Download All Natives
        uses: actions/download-artifact@v4
        with:
          path: native-libs
          pattern: native-*
          merge-multiple: true

      - name: Prepare Resources
        shell: bash
        run: |
          mkdir -p ecobridge-java/src/main/resources/
          cp native-libs/* ecobridge-java/src/main/resources/
          echo "✅ 堆外内存驱动组件已就绪"

      - name: Build with Gradle
        working-directory: ecobridge-java
        run: |
          chmod +x gradlew
          export GRADLE_OPTS="-Dorg.gradle.jvmargs=--enable-preview"
          ./gradlew build -x test

      - name: Final Release
        uses: actions/upload-artifact@v4
        with:
          name: EcoBridge-Java25-Final
          path: ecobridge-java/build/libs/EcoBridge-*.jar
          retention-days: 7